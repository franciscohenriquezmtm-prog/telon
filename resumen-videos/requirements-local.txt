# Modo local (--local): transcripción sin API.  Instalar con: pip install -r requirements-local.txt
# (además de requirements.txt).  El modelo de Whisper se descarga la primera vez desde huggingface.co.
faster-whisper
